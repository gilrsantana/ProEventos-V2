import { DatePipeBrPipe } from './date-pipe-br.pipe';

describe('DatePipeBrPipe', () => {
  it('create an instance', () => {
    const pipe = new DatePipeBrPipe();
    expect(pipe).toBeTruthy();
  });
});
